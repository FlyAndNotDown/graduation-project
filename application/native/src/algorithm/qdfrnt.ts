import * as Mathjs from 'mathjs';

export class QDFRNT {
    public static dfrntKernel(order: number, cycle: number, randomMatrix: number[][]): Mathjs.Matrix {
        // get size info
        let rows: number = randomMatrix.length;
        
        // TODO
        return null;
    }
}